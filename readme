# commands and steps to install
# add .env in the root (.env in email)
# the next list, are commands to execute in bash to launch the api
# docker-compose up
# python -m venv env
# .\env\Scripts\Activate
# pip install -r requirements.txt
# uvicorn app.main:app --reload

# credentials are in the .env
